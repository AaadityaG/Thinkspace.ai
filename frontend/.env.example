VITE_GOOGLE_CLIENT_ID=

# Backend base URL. Use this while running backend + frontend together, e.g. http://localhost:8008.
# Leave empty to use Vite's `/api` dev-server proxy instead (also targets the backend on 8008).
VITE_API_URL=http://localhost:8008
